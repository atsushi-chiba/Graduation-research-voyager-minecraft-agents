# 引き継ぎ 2026-08-29 — オープンキャンパス展示branch

## 安全なソース復元点

- 無変更branch: `codex/open-campus-pre-persona-2026-08-30`
- fallback branch: `codex/open-campus-demo-fallback-2026-08-30`
- base commit: `4c9a7fc2d28fb3bd72b709d3ba4325eb47efc431`
- base tree: `157bbe7b1bab7bcf62e58d8e0b8cd30b094e781e`
- 無変更branchはbaseとcommit/tree hash一致、`git diff 4c9a7fc...codex/open-campus-pre-persona-2026-08-30`は空。

`4c9a7fc`はP1基盤とPhase 1/2の観測コードを含むが、Phase 3 appraisal、時間感情、身体化援助より前の、文書化・ライブ検証済みcheckpointである。展示では関連Node daemonを一切起動しない。

## fallbackの変更範囲

- `DEMO_MODS_DIR`によるBridge compile-time依存jarパスの上書き。
- `demo_preflight.sh`、`demo_healthcheck.sh`、`demo_start.sh`、`demo_stop.sh`。
- `OPEN_CAMPUS_DEMO.md`と週次報告。

研究機能、Bridge endpoint、world、ペルソナ状態、社会状態は変更していない。

## 検証

- 無変更版: JDK 17 build成功、Node 32 tests PASS、Forge/Bridge起動、`/ping`、`/status`成功。
- fallback: 環境変数化build、JAR backup、独立起動、healthcheck、正常停止、JAR復元までPASS。
- ライブ取得: `NormalActual`、35市民、67建物。Forge 1、対象daemon 0。
- tickrateは一時1倍固定後にadaptiveへ復元できた。
- MinecraftクライアントGUI接続だけは未検証。

## 現在のライブ状態

- Forge/Bridge停止中。
- persona/social/supply/council daemon停止中。
- `/root/mc-server-forge/mods/voyagerbridge-0.1.0.jar`は検証前hash
  `317932453c813d088263f5e72378c099aa440cd551ef0008dfc2c044361e4a29`へ復元済み。
- 既存未追跡`compare_metrics.js`、`ops.js`、`zone_audit.js`は変更・移動・stageしていない。

## 展示当日の順序

1. `OPEN_CAMPUS_DEMO.md`どおり無変更branchを試す。
2. build pathまたは起動process回収で失敗した場合だけfallbackへ切り替える。
3. `demo_preflight.sh` → `demo_start.sh` → `demo_healthcheck.sh`。
4. Minecraft接続後、標準AI、`/ping`、`/status`、一時tickrate切替を5〜10分で示す。
5. `demo_stop.sh --restore`で正常停止し、元Bridgeへ戻す。

実験機で最初の通常起動は親process回収によりready直後にJavaが終了した。fallbackの`setsid`独立起動で解消した。これはソース機能不良ではなく実行環境差である。
